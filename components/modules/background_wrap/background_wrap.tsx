import React, { FunctionComponent } from 'react'
import { Page_Flexiblelayout_PageBuilder_BackgroundWrap } from 'interfaces/types'

export const typename = 'Flexiblelayout_PageBuilder_BackgroundWrap'

const BackgroundWrapModule: FunctionComponent<{ module: Page_Flexiblelayout_PageBuilder_BackgroundWrap }> = () => <span className="background-mark" />

export default BackgroundWrapModule
